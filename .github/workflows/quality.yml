name: Quality

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  static-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm run lint

  smoke:
    needs: static-checks
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: test-1
    env:
      BASE_URL: ${{ vars.BASE_URL }}
      API_URL: ${{ vars.API_URL }}
      USER_EMAIL: ${{ vars.USER_EMAIL }}
      USER_NAME: ${{ vars.USER_NAME }}
      USER_PASSWORD: ${{ secrets.USER_PASSWORD }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      # GitHub-hosted runner IPs can receive Cloudflare challenges from the public demo.
      - run: npm run test:ci:smoke
      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: smoke-playwright-report
          path: artifacts/playwright-report/
          retention-days: 30

  regression:
    needs: static-checks
    runs-on: ubuntu-latest
    timeout-minutes: 45
    environment: test-1
    env:
      BASE_URL: ${{ vars.BASE_URL }}
      API_URL: ${{ vars.API_URL }}
      USER_EMAIL: ${{ vars.USER_EMAIL }}
      USER_NAME: ${{ vars.USER_NAME }}
      USER_PASSWORD: ${{ secrets.USER_PASSWORD }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      # Keep real authentication in local suites; required CI runs stable public coverage only.
      - run: npm run test:ci:regression
      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: regression-playwright-report
          path: artifacts/playwright-report/
          retention-days: 30
